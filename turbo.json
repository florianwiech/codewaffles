{
  "baseBranch": "origin/main",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".dist/**", "./build/**", ".webpack/**"]
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": ["coverage/**"]
    },
    "lint": {},
    "lint:ci": {},
    "check:types": {
      "dependsOn": ["^build"]
    },
    "dev": {
      "cache": false
    }
  }
}
