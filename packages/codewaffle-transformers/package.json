{
  "private": true,
  "name": "@codewaffle/transformers",
  "version": "0.0.0",
  "type": "module",
  "main": "dist/index.cjs",
  "exports": {
    "import": "./dist/index.js",
    "require": "./dist/index.cjs"
  },
  "types": "dist/index.d.ts",
  "module": "dist/index.js",
  "sideEffects": false,
  "scripts": {
    "clean": "rimraf dist",
    "build": "yarn clean && codewaffle-scripts build",
    "test": "codewaffle-scripts test",
    "lint": "eslint src --format friendly",
    "lint:stage": "eslint src --format friendly --max-warnings=0",
    "lint:fix": "eslint src --format friendly --fix",
    "check:types": "tsc --noEmit"
  },
  "dependencies": {
    "decode-jwt": "^1.0.2"
  },
  "devDependencies": {
    "@codewaffle/buildhelper": "*",
    "eslint": "^8.7.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.5.5"
  }
}
